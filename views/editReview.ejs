
<%- include("partials/header.ejs") %>

    <div class="reviewsPage">
    
        <main class="mainContainer">

            <div class="container">
                <h2 class="title"><%= review.movie %></h2>
                <div class="movieContainer"> 
                    <img src="<%= review.poster %>" alt="" class="moviePoster">
                </div>
            </div>
            
            <div class="reviewCard">
                <h3 class="cardHeader">Review:</h3>
                <textarea name="review" id="review" cols="40" rows="8" placeholder="Review goes here"> <%= review.review %> </textarea>
                <div class="starContainer">    

                    <h3 class="subHeader">Rating out of 5:</h3>
                    <div class="rate">
                        <input type="radio" id="star5" name="rate" value="5" <% if(review.rating === 5){ %>
                            checked
                            <%} %> >
                            <label for="star5" title="text">5 stars</label>
                            <input type="radio" id="star4" name="rate" value="4" <% if(review.rating === 4){ %>
                                checked
                            <%} %>  />
                            <label for="star4" title="text">4 stars</label>
                            <input type="radio" id="star3" name="rate" value="3"  <% if(review.rating === 3){ %>
                                checked
                            <%} %> />
                            <label for="star3" title="text">3 stars</label>
                            <input type="radio" id="star2" name="rate" value="2" <% if(review.rating === 2){ %>
                                checked
                            <%} %>  />
                            <label for="star2" title="text">2 stars</label>
                            <input type="radio" id="star1" name="rate" value="1" <% if(review.rating === 1){ %>
                                checked
                                <%} %> />
                                <label for="star1" title="text">1 star</label>
                            </div>
                        </div>
                        <div class="starContainer">
                        




                    </div>
                </div>
                <button class="btn button" id="update" data-id="<%= review.id%>">Update Review</button>
            </div>
        </main>
    </div>
      
    
    <script src="../../js/review.js"></script>
    <%- include("partials/footer.ejs") %>

